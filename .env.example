# Base URL of the web-admin-backend service
VITE_API_BASE_URL=http://localhost:3001

# Baseline app credential required by the backend ClientMiddleware.
# Must match TOKEN_CLIENT in web-admin-backend/.env (default: clarateClientId)
VITE_CLIENT_TOKEN=clarateClientId
